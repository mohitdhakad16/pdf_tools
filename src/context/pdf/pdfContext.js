import { createContext } from "react";  //importing createContext from React

const pdfContext = createContext();    // storing the createContext function in the noteContext variable

export default pdfContext;